using UnityEngine;
using System.Collections;

public enum ColorPathType
{
    Green = 0,
    Blue,
    Red,
    Yellow,
    Purple,
    Orange
}

public enum PlayerType
{
    LocalPlayer = 0,
    BotPlayer,
    ForeignPlayer
}
